/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		remotePatterns: [
			{
				protocol: 'https',
				hostname: 'cpszatorchxzodumpccx.supabase.co'
			}
		]
	}
};

export default nextConfig;
